<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" 
              data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav navbar-right">

        <li class="<%= 'active' if params[:controller] == 'resume' %>"><a href="/resume">Résumé</a></li>
        <li class="<%= 'active' if params[:controller] == 'projects' %>"><a href="/projects">Projects</a></li>
        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" 
               role="button" aria-haspopup="true" aria-expanded="false">
               <%= @current_user.name %><span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li class="<%= 'active' if params[:controller] == 'users' && params[:action] == 'show' %>">
                  <%= link_to_fa "Profile", current_user, "user" %></li>
              <li><%= link_to_fa "Settings", edit_user_path(current_user), "gear" %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to '<i class="fa fa-sign-out"></i>&nbsp; Log Out'.html_safe, logout_path, method: "delete" %></li>
            </ul>
          </li>
        <% end %>


      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>